# ── Telegram ──────────────────────────────────────────────
pyrogram==2.0.106
tgcrypto                  # Faster crypto for Pyrogram (must have)

# ── Database ──────────────────────────────────────────────
motor==3.3.2              # Async MongoDB driver
pymongo[srv]==4.6.1       # MongoDB (srv for Atlas connection strings)
dnspython                 # Required for mongodb+srv:// URIs

# ── Config / Env ──────────────────────────────────────────
python-dotenv==1.0.1      # Load .env variables

# ── HTTP / Downloading ────────────────────────────────────
aiohttp==3.9.5            # Async HTTP (image download etc.)
aiofiles==23.2.1          # Async file read/write
requests==2.31.0          # Sync HTTP (fallback use)
httpx==0.27.0             # Modern HTTP client

# ── Image Processing ──────────────────────────────────────
Pillow==10.3.0            # Image handling if needed later

# ── Utilities ─────────────────────────────────────────────
loguru==0.7.2             # Better logging
humanize==4.9.0           # Human readable numbers/dates
python-dateutil==2.9.0    # Date parsing
pytz==2024.1              # Timezone support

